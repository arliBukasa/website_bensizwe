<?xml version="1.0" encoding="utf-8"?>
<odoo>
    <data noupdate="0">
        <!-- Force la mise à jour des templates de réinitialisation -->
        <record id="candidat_reset_password_template" model="ir.ui.view">
            <field name="name">Candidat Reset Password</field>
            <field name="key">website_bensizwe.candidat_reset_password_template</field>
            <field name="type">qweb</field>
            <field name="arch" type="xml">
                <t t-name="website_bensizwe.candidat_reset_password_template">
                    <t t-call="website.layout">
                        <div class="container mt-5 mb-5">
                            <div class="row justify-content-center">
                                <div class="col-md-6">
                                    <div class="card shadow-sm">
                                        <div class="card-body">
                                            <h3 class="mb-4 text-center">Réinitialiser le mot de passe</h3>
                                            
                                            <div class="text-center mb-4">
                                                <i class="fa fa-lock fa-3x text-muted mb-3"></i>
                                                <p class="text-muted">Entrez votre adresse email pour recevoir un lien de réinitialisation</p>
                                            </div>

                                            <t t-if="error">
                                                <div class="alert alert-danger" role="alert">
                                                    <t t-esc="error" />
                                                </div>
                                            </t>

                                            <t t-if="success">
                                                <div class="alert alert-success" role="alert">
                                                    <i class="fa fa-check-circle me-2"></i>
                                                    Un email de réinitialisation a été envoyé à votre adresse.
                                                </div>
                                            </t>

                                            <form method="post" action="/candidat/reset_password/submit">
                                                <div class="mb-3">
                                                    <label for="email" class="form-label">Adresse email</label>
                                                    <input type="email" name="email" class="form-control" placeholder="votre.email@exemple.com" required="required"/>
                                                </div>
                                                <div class="d-grid">
                                                    <button type="submit" class="btn btn-primary">
                                                        <i class="fa fa-paper-plane me-2"></i>Envoyer le lien
                                                    </button>
                                                </div>
                                            </form>

                                            <div class="text-center mt-3">
                                                <a href="/candidat/login">Retour à la connexion</a>
                                            </div>
                                        </div>
                                    </div>
                                </div>
                            </div>
                        </div>
                    </t>
                </t>
            </field>
        </record>
    </data>
</odoo>